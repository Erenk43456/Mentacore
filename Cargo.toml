[workspace]
resolver = "2"

members = [
    "bootloader",
    "boot_protocol",
    "kernel",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"
authors = ["Eren"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"